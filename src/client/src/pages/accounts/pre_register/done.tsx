import React from 'react';
import { PreRegisterDoneContainer } from 'src/components/accounts/preRegister/done';

const done = () => {
  return (
    <div>
      <PreRegisterDoneContainer />
    </div>
  );
};

export default done;
